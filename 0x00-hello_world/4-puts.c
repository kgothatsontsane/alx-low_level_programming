#include <stdio.h>

/**
 * main - prints the string in puts
 *
 * Return = 0
 */
int main(void)
{
	int puts(const char *s);
	puts("Programming is like building a multilingual puzzle");

	return (0);
}
